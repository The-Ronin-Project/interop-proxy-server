query {
    getPractitionerByProvider(tenantId: "__tenant_mnemonic__", providerId: "E1000") {
        id
        identifier {
            system
            value
        }
        name {
            family
            given
            use
        }
    }
}
