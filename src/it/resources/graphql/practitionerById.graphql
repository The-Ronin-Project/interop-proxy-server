query {
    getPractitionerById(tenantId: "__tenant_mnemonic__", practitionerFhirId: "__id__") {
        id
        identifier {
            system
            value
        }
        name {
            family
            given
            use
        }
    }
}
